/**
 * Набір констант внутрішнього gateway-протоколу.
 *
 * Призначення:
 * - зберігати перелік дозволених операцій протоколу;
 * - зберігати перелік доступних категорій / предметів,
 *   які використовуються у шаблонах рейтингової підсистеми;
 * - винести ці значення в окремий файл, щоб їх можна було
 *   централізовано використовувати у валідаторах, детекторі шаблонів
 *   та інших сервісах шлюзу.
 */

/**
 * Список дозволених операцій внутрішнього текстового протоколу.
 *
 * Використовується для перевірки, що запит містить
 * підтримуваний метод взаємодії.
 *
 * Підтримувані операції:
 * - GET    — отримання даних
 * - PUT    — повна зміна значень
 * - PATCH  — часткова зміна значень
 * - DELETE — видалення даних
 *
 * @type {string[]}
 */
const ALLOWED_OPERATIONS = ["GET", "PUT", "PATCH", "DELETE"];

/**
 * Перелік доступних категорій рейтингу.
 *
 * У цьому масиві зібрані технічні та soft-skill категорії,
 * які система визнає допустимими для шаблонів рейтингової підсистеми.
 *
 * Приклади технічних категорій:
 * - мови програмування
 * - робота з базами даних
 * - алгоритми
 * - структури даних
 *
 * Приклади нетехнічних категорій:
 * - командна взаємодія
 * - комунікація
 * - відповідальність
 * - ініціативність
 * - лідерство
 *
 * @type {string[]}
 */
const AVAILABLE_SUBJECTS = [
  "LANG_JAVA",
  "LANG_CPP",
  "LANG_CS",
  "LANG_JS",
  "LANG_PYTHON",
  "MY_SQL",
  "POSTGRES_SQL",
  "DB_DESIGN",
  "ALGORITHMS",
  "DATA_STRUCTURES",
  "SYSTEM_ANALYSIS",
  "TEAMWORK",
  "COMMUNICATION",
  "RESPONSIBILITY",
  "INITIATIVE",
  "PROBLEM_SOLVING",
  "CREATIVITY",
  "LEADERSHIP",
];

/**
 * Експортуються константи, що використовуються
 * в інших модулях шлюзу.
 */
module.exports = {
  ALLOWED_OPERATIONS,
  AVAILABLE_SUBJECTS,
};